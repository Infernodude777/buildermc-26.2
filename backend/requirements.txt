# builderMC Python backend dependencies
# FastAPI + ASGI server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Schematic generation
mcschematic>=1.3.0

# Validation & config
pydantic>=2.6.0
pydantic-settings>=2.1.0

# Real LLM provider (NVIDIA NIM — OpenAI-compatible endpoint, uses httpx)
httpx>=0.27.0
